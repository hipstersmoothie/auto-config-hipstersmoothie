{
  "plugins": ["npm", "released"],
  "labels": {
    "dependencies": {
      "name": "dependencies",
      "title": "🔩 Dependency Updates",
      "description": "The change only effects the dependencies."
    }
  },
  "skipReleaseLabels": ["dependencies"]
}
